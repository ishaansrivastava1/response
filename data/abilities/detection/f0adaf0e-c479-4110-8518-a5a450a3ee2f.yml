---

- id: f0adaf0e-c479-4110-8518-a5a450a3ee2f
  name: Find Suspicious IPs
  description: Discover IPs that connected to the honeypot port
  tactic: detection
  technique:
    attack_id: x
    name: x
  platforms:
    linux:
      sh:
        command: |
          cat honeyport.txt | grep -i 'Connection from' | awk '{print $3}'
        parsers:
          plugins.response.app.parsers.basic_strip:
            - source: remote.suspicious.ip
    darwin:
      sh:
        command: |
          cat honeyport.txt | grep -i 'Connection from' | awk '{print $3}'
        parsers:
          plugins.response.app.parsers.basic_strip:
            - source: remote.suspicious.ip